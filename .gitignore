.env
token.json
credentials.json
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
dist/
.venv/
venv/

# Claude Code / planning artifacts — keep local, never commit
.superpowers/
docs/superpowers/

profile.yaml

# SQLite run log (local); on Actions it lives in the `data` branch
data/
